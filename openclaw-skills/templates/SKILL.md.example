---
name: your-skill-name
description: |
  用一段话描述这个 Skill 的功能。description 的覆盖度决定了自然语言能否触发 Skill，
  所以要包含：核心功能关键词、用户可能的提问方式、相关概念的中英文表述。

  示例：定期巡检加密货币行情，获取 BTC、ETH、LTC 的实时价格和涨跌幅，
  超过阈值时推送告警摘要。支持斜杠命令 /your-skill-name 或自然语言触发。
metadata:
  openclaw:
    requires:
      env:
        - YOUR_API_KEY          # 必需的环境变量（存储在 ~/.openclaw/.env）
        - OPTIONAL_THRESHOLD    # 可选变量，在下方说明默认值
      bins:
        - curl                  # 必需的 CLI 工具
        - jq                    # 用于 JSON 解析
---

# Your Skill Name — 一句话说明

## 环境变量

| 变量名 | 必需 | 默认值 | 说明 |
|--------|------|--------|------|
| `YOUR_API_KEY` | ✅ | — | 你的 API 密钥 |
| `OPTIONAL_THRESHOLD` | ❌ | `3` | 告警阈值（百分比） |

## 执行步骤

1. 读取环境变量，检查必需变量是否存在
2. 调用外部 API 获取数据
   ```bash
   curl -s "https://api.example.com/data?api_key=${YOUR_API_KEY}"
   ```
3. 解析返回数据，提取关键指标
4. 与阈值比较，决定输出内容

## 异常处理

### 超时 / 网络错误
- API 调用失败时，等待 **10 秒** 后重试 **1 次**
- 仍然失败则报告错误：`❌ API 调用失败: [错误信息]`

### 限流（HTTP 429）
- 收到 429 响应时，等待 **60 秒** 后重试
- 仍返回 429 则报告：`⚠️ API 限流，请稍后重试`

### 数据异常
- 返回数据格式不符合预期时，报告具体字段缺失信息
- 不要静默忽略错误

## 输出格式

### 有告警时
输出结构化摘要：

```
📊 [Your Skill] 巡检报告

⚠️ 异动项目：
- 项目A：当前值 xxx（变动 +5.2%，超过阈值 3%）

✅ 正常项目：
- 项目B：当前值 yyy（变动 +1.1%）

⏰ 检查时间：2024-01-01 12:00 UTC
```

### 全部正常时
```
HEARTBEAT_OK
```

## 停止条件

- 全部指标在阈值范围内 → 输出 `HEARTBEAT_OK`，任务完成
- 有指标超过阈值 → 输出告警摘要，任务完成
- API 多次失败 → 输出错误报告，任务完成
